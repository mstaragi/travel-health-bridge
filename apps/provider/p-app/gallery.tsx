import React from 'react';
import { Gallery } from '@travelhealthbridge/shared/ui';

/**
 * Gallery Route — /gallery
 * Shows all UI components in all states for visual verification.
 * Accessible at: http://localhost:8099/gallery
 */
export default function GalleryScreen() {
  return <Gallery />;
}
